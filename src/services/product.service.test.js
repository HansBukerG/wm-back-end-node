import { Read } from './product.service';

describe('Should get all reads OK', () =>{
    test('Should code receive 202 status OK', async () => {
        // expect.assertions(1)
        // const { code } = await productService.read()
        // expect(code).toBe(202);
    })
})